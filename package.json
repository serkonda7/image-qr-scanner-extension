{
  "name": "image-qr-scanner-extension",
  "license": "MPL-2.0",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "pack": "bun run build && bun run scripts/pack.ts",
    "lint": "biome check --write",
    "lint:ci": "biome ci",
    "check": "tsc",
    "test": "E2E_TEST=1 bun run build && bun test tests/ --timeout 30000"
  },
  "packageManager": "bun@1.4.0",
  "devDependencies": {
    "@biomejs/biome": "2.5.11",
    "@types/bun": "1.4.0",
    "@types/chrome": "~0.2.7",
    "@types/node": "~26.4.0",
    "jszip": "~3.10.1",
    "playwright": "~1.62.1",
    "typescript": "~7.0.2"
  },
  "dependencies": {
    "jsqr": "~1.4.0"
  }
}
